spring:
  datasource:
    url: jdbc:mysql://db:3306/raceportal?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: raceportal
    password: raceportal

raceportal:
  seed-on-startup: true
